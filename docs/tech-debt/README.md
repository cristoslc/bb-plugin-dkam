# Tech debt

Pre-existing LSP errors, lint violations, type errors, and other debt too
complex to fix inline. Entries follow `~/.agents/agents-md-detail/lsp-errors.md`
(entry format, deletion policy, running index).

Minor/single-file errors are fixed inline in a chore commit; only significant
or multi-file errors are logged here. Empty for now.