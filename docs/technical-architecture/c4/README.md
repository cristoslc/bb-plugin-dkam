# C4 diagrams

C4 level diagrams for DKAM, one file per level:

- `context.md` — system context: operator, BB server, agent-graph, TTS
- `container.md` — plugin server entry, drift watcher, phone channel page
- `component.md` — (pending) components inside the plugin entry; until the
  drift watcher and phone channel land, detail lives inline in `server.ts`
- `deployment.md` — where DKAM runs and how the phone reaches it

Hub: [../../ARCHITECTURE.md](../../ARCHITECTURE.md).