# Developer workflows (spoke docs)

Build, test, deploy, CI/CD detail beyond the
[DEVELOPER-WORKFLOWS.md](../DEVELOPER-WORKFLOWS.md) hub. Empty for now — the
hub carries the current state (build commands, install/reload loop, TODO test
command, two-stage pipeline scripts in `scripts/staging/`).